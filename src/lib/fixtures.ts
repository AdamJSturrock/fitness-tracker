// Wave 1 placeholder. Wave 2 Agent U fills this with mock profiles, entries,
// foods, and meal_items so the UI pages can render against fake data while
// Agent B builds the real DB layer.
//
// Keep this file importable so other modules don't fail to resolve.

export const empty = true;
